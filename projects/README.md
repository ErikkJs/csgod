# Projects

Standalone showcase projects outside of coursework. **Languages:** C++.

| Folder | What it is |
|--------|------------|
| `linked-lists` | C++ singly linked list class with insert-at-head/tail, traversal print, and destructor cleanup |
| `rambo` | Team SDL/OpenGL 2D shooter game (SW Engineering 3350) with sprites, sound, and an online leaderboard — see its own README |
| `timecomparisons` | if-else chain vs switch codegen benchmark: paired C++ sources and their generated x86 `.asm` showing the compiler's jump-table output |
