# Python Coursework

CMPS 3500 Lab 13 on higher-order functions and generator coroutines. **Languages:** Python.

| Folder | What it is |
|--------|------------|
| `lab13-higher-order-generators/` | Lab 13 (Python 2): hand-rolled `map`/`filter`/`mean` plus a `yield`-based producer/consumer coroutine job queue with add/remove, with its `lab13.data` / `names.txt` sample inputs. |
